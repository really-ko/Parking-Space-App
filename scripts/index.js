console.log('WebSocket client script will run here.');

var update_color (row, col) {
  //loop through table to find cell
  // for

  //update cell color
  var x = document.getElementById("spot11");
  x[0].innerHTML = "changing cell color";
  x[0].style.backgroundColor = "red";
};
